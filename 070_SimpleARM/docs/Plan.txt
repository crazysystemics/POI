Simple ARM is an initial pilot for to defence project. It should evolve into VN project.
Now, the threads are
	Optimal Path Finding for Fighter Aircraft (Simple ARM)
	1A.[DONE] Find optimal pdist in presence of no errors
	1B.[+]Prove pdist is optimal when no errors are present
	        1B1. Find detection_count for pymin to pmax in steps of pstep
			         int[]  detection_counts = find_detection_counts(pymin, pmax, pstep)
			1B2. Verify the assertion that detection_count(pdist) is minimmum 
			      among all detection_count(py) for py in [pymin, pmax] with step size of pstep
				       [***] write a function to find detection_count for a given py
					   [***] min_detection = find_index_of_minimum(detection_counts)
					   [***] assert that pymin + min_detection*pstep == pdist
					   
]			[1B3] [***] Generate code for 1B1 and 1B2
	2. Find Optimal y in presence of errors for Radar Position in x 
	         a. ideally, this should not impact y
			 b. i.e., irrespective of error (from min_error to max_error), y should be same
			 c. verify this
			 d. now, go to code
	3. Based on Aho, Ullman Abstraction Article         
			  
			   
			  a. define (aircraft with rwr, radar) as an abstraction
			  b. define a mission as an algorithm that takes in the abstraction and produces a result (e.g., optimal y)
			  c. implement manually aircrraft, rwr and radar
			  d. DTSE->dtse is the compiler/simulator that runs the mission and generates effectiveness score of y
			  e. alternate paths/uncertainty in each iteration, data should be captured as shown below
			            example vector of optimal y for each position error
			  i. Algorithm: find_optimal_y and validate_optimal_y 			       
			 j. n-dimensions 
			          [on each dimension, n samples can be sampled, n may be 1 also]
					  [if nd > 1, it is a range, and sample is linear, 
					               xd (x on d) is tranversed from min to max with step size of (max - min)/n]
					  [if nd > 1, and sampling is random, then xd is sampled randomly from min to max n times]
					  [nd = 1 linear can be modeled by mid = maxd = some fixed xd value]
					  [nd = 1 random -;> xd is sampled randomly from min to max]

					  //foreground dimensions - dimensions on which output is dependent
					  //these are tested for nd samples, ranges are covered
					  //for random variables, six sigma is used to cover the range
					  //background dimensions - dimensions on which output is not dependent
					  //a single random sample is drawn from each of the background dimensions, and 
					  //in same output iteration output is computed.
					  //if background dimensions indeed have no impact on output, 
					  //then output should be same across all samples of background dimensions
			k. Aho Ullman Abstraction Framework also has to-be incorporated
