
    1. DONE
	2. Find Optimal y in presence of errors for Radar Position in x 
	         a. ideally, this should not impact y
			 b. Radar.x_error is the error associated with radar position in x
			 b. Radar.x can be anywhere from Radar.x - Radar.x_error to Radar.x + Radar.x_error
			 c. Assuming aircraft's flight envelope is superset of radar visibility zone,
			      Radar's positional variation in x should not affect choice of y
			 d. Validate this
			 e. Also, show that if aircraft's flight envelope is not superset of radar visibility zone, 
			           then Radar's positional variation in x can affect choice of y
	3. Same logic as above, there can be error in position on Radar Y also.
	4. Radar position's error is probabilistic
	         a. Radar position x's error distribution specified in Application Note.
	         b. How to compute visibility zone and optimal y in this case?
			       Every sample will be drawn from distribution, 
				   and for each sample, visibility zone will be computed, 
				   and optimal optimal_y for that should be computed.
			b.  let  visibility_zone_set =   visibility zones fron n random sample positions 
				                                corresponding to n samples of 
												radar position errors
			c.  average_visibility_zone = average of visibility zones in visibility_zone_set
			d.  optimal_y = optimal y for average_visibility_zone (based on overlapping rectangle logic)
			e.  validate that optimal_y is indeed optimal for all visibility zones in visibility_zone_set
			          compute count_better_than_optimal_y
					  compute  count_worse_than_optimal_y
					  some of both better and worse count should be #visibility_zone_set
			f. Further validation can be done by drawing different samples.			 								 

	